import 'package:flutter/material.dart';

void main() => runApp(Kurakani());

class Kurakani extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(title: "Kurakani");
  }
}
